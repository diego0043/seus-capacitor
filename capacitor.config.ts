import { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.example.app",
  appName: "seus-capacitor",
  webDir: "index.template.html",
  bundledWebRuntime: false,
};

export default config;
